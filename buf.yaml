version: v1beta1

name: buf.build/batazor/shortlink

deps:
  - buf.build/beta/googleapis

build:
  roots:
    - internal/services/proxy
  excludes:
    - internal/services/proxy/node_modules

lint:
  use:
    - DEFAULT # the current strictest possible

breaking:
  use:
    - WIRE_JSON

