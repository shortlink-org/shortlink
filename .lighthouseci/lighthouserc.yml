ci:
  collect:
    url: ["http://localhost:3000/"]
    startServerCommand: 'cd iu/next && npm start'
  assert:
    preset: 'lighthouse:recommended',
    assertions:
      uses-rel-preload: 'off'
      uses-rel-preconnect: 'off'
  upload:
    # upload options here
  server:
    # server options here
  wizard:
    # wizard options here
