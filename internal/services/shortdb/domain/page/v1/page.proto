syntax = "proto3";

package shortdb.domain.page.v1;

option go_package = "github.com/shortlink-org/shortlink/internal/services/shortdb/domain/page/v1";

message Row {
  map<string, bytes> value = 2;
}

message Page {
  repeated Row rows = 1;
}
