- name: Install common dependencies
  hosts: all
  become: True
  vars:
    pip_package: python3-pip
    pip_install_packages:
      - name: docker
  vars_files:
    - "./group_vars/values.yml"
  roles:
    - geerlingguy.pip
    - geerlingguy.docker
    - nginx
    - ui-nuxt
