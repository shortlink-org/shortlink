[defaults]
inventory = application.host
remote_user = vagrant
private_key_file = ~/.vagrant.d/insecure_private_key
; Need enable for production
host_key_checking = False
ansible_python_interpreter = /usr/bin/python3
