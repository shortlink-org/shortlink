version: '3.7'

services:

  postgres:
    image: postgres:alpine
    init: true
    restart: on-failure
    container_name: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: shortlink
      POSTGRES_PASSWORD: shortlink
      POSTGRES_DB: shortlink
    volumes:
      - ./initialState/init-postgres.sql:/docker-entrypoint-initdb.d/init.sql
