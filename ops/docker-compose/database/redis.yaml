version: '2.4'

services:

  redis:
    image: redis:alpine
    init: true
    restart: on-failure
    container_name: redis
    ports:
      - "6379:6379"
    networks:
      - simple
    dns:
      - "10.5.0.2"
    dns_search: coredns
