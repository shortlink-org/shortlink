apiVersion: 1

providers:
  - name: 'Shortlink'
    folder: 'Shortlink'
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/shortlink/postgres

  - name: 'RPC'
    folder: 'RPC'
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/rpc

  - name: 'Alerts'
    folder: 'Alerts'
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/alerts

  - name: 'Logs'
    folder: 'Logger'
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/logger

  - name: 'Infrastructure'
    folder: 'Infrastructure'
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/infrastructure
