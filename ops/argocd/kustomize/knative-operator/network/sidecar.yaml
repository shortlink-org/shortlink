apiVersion: networking.istio.io/v1beta1
kind: Sidecar
metadata:
  name: default
spec:
  egress:
    - hosts:
      - "./*"                # current namespace
      - "istio-system/*"     # istio-system services
      - "knative-serving/*"  # knative-serving services
      - "knative-eventing/*" # knative-eventing services
      - "kafka/*"            # kafka services
      - "grafana/*"          # grafana services
