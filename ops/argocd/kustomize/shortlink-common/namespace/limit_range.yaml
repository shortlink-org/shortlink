apiVersion: v1
kind: LimitRange
metadata:
  name: cpu-memory-limits
spec:
  limits:
    - type: Container
      max:
        cpu: "2"
        memory: 2Gi
      min:
        cpu: 2m
        memory: 5Mi
