apiVersion: authzed.com/v1alpha1
kind: SpiceDBCluster
metadata:
  name: shortlink
spec:
  config:
    replicas: 1
    datastoreEngine: memory
  presharedKey:
    key: preshared_key
    secretName: shortlink-spicedb-config
  datastoreUri:
    secretName: spicedb-postgres-pguser-spicedb
    key: uri
