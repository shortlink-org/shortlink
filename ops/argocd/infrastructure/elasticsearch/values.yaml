clusterName: shortlink

replicas: 1
minimumMasterNodes: 1

# Shrink default JVM heap.
esJavaOpts: "-Xmx128m -Xms128m"

resources:
  requests:
    cpu: 100m
    memory: 256Mi

persistence:
  enabled: false

ingress:
  enabled: false
