# NOTE:
# APPLICATION   - Name application
# PACKAGE_PATH  - Path to package.json
.matrix_build_npm:
  parallel:
    matrix:
      - APPLICATION: switch-mode
        PACKAGE_PATH: "ui/pkg/switch-mode"
