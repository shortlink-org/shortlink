# CERT-MANAGER =========================================================================================================
cert-manager:
  stage: gateway
  variables:
    RELEASE_NAME: cert-manager
    HELM_PATH: ops/Helm/addons/cert-manager
    HELM_NAMESPACE: cert-manager
    # $GITLAB_USER_EMAIL - no production-ready
    HELM_ARG: --set email=$GITLAB_USER_EMAIL
  trigger:
    include:
      - local: 'ops/gitlab/pipelines/helm_deploy.yml'
    strategy: depend
  needs: []
  rules:
    - when: manual

# NGINX-INGRESS ========================================================================================================
nginx-ingress:
  stage: gateway
  variables:
    RELEASE_NAME: nginx-ingress
    HELM_PATH: ops/Helm/addons/nginx-ingress
    HELM_NAMESPACE: nginx-ingress
  trigger:
    include:
      - local: 'ops/gitlab/pipelines/helm_deploy.yml'
    strategy: depend
  needs: []
  rules:
    - when: manual

# ISTIO ================================================================================================================
istio:
  stage: gateway
  variables:
    RELEASE_NAME: istio-base
    HELM_PATH: ops/Helm/addons/gateway/istio
    HELM_NAMESPACE: istio-system
  trigger:
    include:
      - local: 'ops/gitlab/pipelines/helm_deploy.yml'
    strategy: depend
  needs: []
  rules:
    - when: manual
