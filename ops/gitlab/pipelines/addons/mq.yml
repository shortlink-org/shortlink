# DATABASE =============================================================================================================
rabbitmq:
  stage: mq
  variables:
    RELEASE_NAME: rabbitmq
    HELM_PATH: ops/Helm/addons/mq/rabbitmq
    HELM_NAMESPACE: rabbitmq
  trigger:
    include:
      - local: 'ops/gitlab/pipelines/helm_deploy.yml'
    strategy: depend
  needs: []
  rules:
    - when: manual

kafka:
  stage: mq
  variables:
    RELEASE_NAME: kafka
    HELM_PATH: ops/Helm/addons/mq/kafka
    HELM_NAMESPACE: kafka
  trigger:
    include:
      - local: 'ops/gitlab/pipelines/helm_deploy.yml'
    strategy: depend
  needs: []
  rules:
    - when: manual

kafka-ui:
  stage: mq
  variables:
    RELEASE_NAME: kafka-ui
    HELM_PATH: ops/Helm/addons/mq/kafka-ui
    HELM_NAMESPACE: kafka
  trigger:
    include:
      - local: 'ops/gitlab/pipelines/helm_deploy.yml'
    strategy: depend
  needs: []
  rules:
    - when: manual
