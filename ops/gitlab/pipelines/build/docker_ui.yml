include:
  - local: /ops/gitlab/pipelines/build/docker_base.yml
  - local: /ops/gitlab/templates/cache_ui.yaml

build:
  extends:
    - .cache
    - .template_build
  script:
    - pwd
    - ls -la ui/nx-monorepo/packages/*
