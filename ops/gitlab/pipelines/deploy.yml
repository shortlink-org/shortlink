include:
  - local: /ops/gitlab/templates/deploy.yml

stages:
  - deploy

deploy:
  extends:
    - .job_template_deploy
  tags:
    - docker
  environment:
    name: $ENVIRONMENT_NAME
    url: $ENVIRONMENT_URL
