# BUILD IMAGE ==========================================================================================================
trigger:build:
  stage: build
  trigger:
    include:
      - local: 'ops/gitlab/pipelines/build.yml'
    strategy: depend
  needs:
    - gosec
    - gotest
    - golint
  rules:
    - if: $CI_COMMIT_TAG
      when: on_success
