# BUILD IMAGE ==========================================================================================================
docker:
  stage: build
  trigger:
    include:
      - local: 'ops/gitlab/pipelines/docker_build.yml'
    strategy: depend
  needs:
    - GOLANG
  rules:
    - if: $CI_COMMIT_TAG
      when: on_success
