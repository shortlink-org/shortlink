# BUILD IMAGE ==========================================================================================================
docker:build:
  extends:
    - .rules_release
  stage: build
  trigger:
    include:
      - local: 'ops/gitlab/pipelines/docker_build.yml'
    strategy: depend
  needs:
    - GOLANG
