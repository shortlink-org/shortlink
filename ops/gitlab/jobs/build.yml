# BUILD IMAGE ==========================================================================================================
docker:build_go:
  # TODO: test
#  stage: build
  stage: .pre
  # TODO: test
  variables:
    CI_COMMIT_TAG: 0.9.33
  trigger:
    include:
      - local: 'ops/gitlab/pipelines/docker_build_go.yml'
    strategy: depend
  # TODO: test
#  needs:
#    - GOLANG
  rules:
    # TODO: test
    - when: always
    - if: $CI_COMMIT_TAG
      when: on_success

docker:build_nodejs:
  stage: build
  trigger:
    include:
      - local: 'ops/gitlab/pipelines/docker_build_nodejs.yml'
    strategy: depend
  needs: []
  rules:
    - if: $CI_COMMIT_TAG
      when: on_success
