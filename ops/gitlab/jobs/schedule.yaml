delete_renovate_branch:
  stage: .pre
  script:
    - git branch -r | awk -Forigin/ '/\/renovate/ {print $2}' | xargs -I {} git push gitlab :{}
  rules:
    - when: always
      if: $CI_PIPELINE_SOURCE == "schedule"

