delete_renovate_branch:
  stage: .pre
  image: alpine
  before_script:
    - apk add --update git
  script:
    - git branch -r | awk -Forigin/ '/\/renovate/ {print $2}' | xargs -I {} git push origin :{}
  rules:
    - when: always
      if: $CI_PIPELINE_SOURCE == "schedule"

