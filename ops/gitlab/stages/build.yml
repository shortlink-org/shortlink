# BUILD IMAGE ==========================================================================================================
docker:
  stage: build
  trigger:
    include:
      - local: 'ops/gitlab/pipelines/build/docker.yml'
    strategy: depend
  needs:
    - job: GOLANG
    - job: DEPENDENCY
  rules:
    - if: $CI_COMMIT_TAG
      when: on_success

helm-chart:
  stage: build
  trigger:
    include:
      - local: 'ops/gitlab/pipelines/build/helm_chart.yml'
    strategy: depend
  needs:
    - job: HELM
  rules:
    - if: $CI_COMMIT_TAG
      when: on_success
