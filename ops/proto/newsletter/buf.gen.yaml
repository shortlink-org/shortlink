version: v1

managed:
  enabled: true

plugins:
  - name: go
    out: internal/services/billing
    opt: paths=source_relative

  - name: go-grpc
    out: internal/services/billing
    opt: paths=source_relative
