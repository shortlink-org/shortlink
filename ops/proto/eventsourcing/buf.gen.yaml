version: v1

managed:
  enabled: true

plugins:
  - plugin: buf.build/protocolbuffers/go
    out: internal/pkg
    opt: paths=source_relative

  - plugin: buf.build/grpc/go
    out: internal/pkg
    opt: paths=source_relative
