version: v1

managed:
  enabled: true

plugins:
  - name: go
    out: internal/services/metadata
    opt: paths=source_relative

  - name: go-grpc
    out: internal/services/metadata
    opt: paths=source_relative
