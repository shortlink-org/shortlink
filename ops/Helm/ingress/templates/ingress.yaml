{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: {{ include "helpers.fullname" . }}
  labels:
    {{ include "helpers.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | indent 4 }}
  {{- end }}
spec:
{{- if .Values.ingress.tls }}
  tls:
  {{- range .Values.ingress.tls }}
    - hosts:
      {{- range .hosts }}
        - {{ . | quote }}
      {{- end }}
      secretName: {{ .secretName }}
  {{- end }}
{{- end }}
  rules:
    - host: {{ .Values.host }}
      http:
        paths:
          - path: /ui
            backend:
              serviceName: shortlink-next
              servicePort: 80
          - path: /next
            backend:
              serviceName: shortlink-next
              servicePort: 80
          - path: /nuxt
            backend:
              serviceName: shortlink-nuxt
              servicePort: 80
          - path: /api
            backend:
              serviceName: shortlink-api
              servicePort: 7070
          - path: /tracing
            backend:
              serviceName: jaeger-agent.jaeger-operator
              servicePort: 6831
{{- end }}
