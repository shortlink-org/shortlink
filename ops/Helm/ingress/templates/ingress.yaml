{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: {{ include "helpers.fullname" . }}
  labels:
    {{- include "helpers.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
{{- if .Values.ingress.tls }}
  tls:
  {{- range .Values.ingress.tls }}
    - hosts:
      {{- range .hosts }}
        - {{ . | quote }}
      {{- end }}
      secretName: {{ .secretName }}
  {{- end }}
{{- end }}
  rules:
    - host: {{ .Values.host }}
      http:
        paths:
          - path: /ui(/|$)(.*)
            pathType: Prefix
            backend:
              serviceName: shortlink-next-shortlink-ui
              servicePort: 80
          - path: /next
            pathType: Prefix
            backend:
              serviceName: shortlink-next-shortlink-ui
              servicePort: 80
          - path: /nuxt(/|$)(.*)
            pathType: Prefix
            backend:
              serviceName: shortlink-nuxt
              servicePort: 80
          - path: /api
            pathType: Prefix
            backend:
              serviceName: shortlink-api
              servicePort: 7070
{{- end }}
