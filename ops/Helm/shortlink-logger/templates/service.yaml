apiVersion: v1
kind: Service
metadata:
  name: {{ include "shortlink.fullname" . }}-logger
  labels:
    {{ include "shortlink.labels" . | nindent 4 }}
spec:
  type: {{ .Values.logger.service.type }}
  ports:
    - port: {{ .Values.logger.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "shortlink.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: logger
