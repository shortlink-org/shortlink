apiVersion: tests.testkube.io/v1
kind: Test
metadata:
  name: shortlink-landing-test-suites
  namespace: testkube
spec:
  description: Test suite for shortlink landing page
  steps:
    - execute:
        name: shortlink-landing-cypress-main-page
    - delay:
        duration: 100
    - execute:
        name: shortlink-landing-k6-main-page
