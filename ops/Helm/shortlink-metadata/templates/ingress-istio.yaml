{{- if and .Values.ingress.enabled (eq .Values.ingress.type "istio") -}}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ include "helpers.fullname" . }}
  labels:
    {{ include "helpers.labels" . | nindent 4 }}
spec:
  hosts:
  - "*"
  gateways:
    - shortlink-ingress
  http:
  - match:
      - port: 50051
    route:
    - destination:
        host: metadata-shortlink-metadata
        port:
          number: 50051
{{- end }}
