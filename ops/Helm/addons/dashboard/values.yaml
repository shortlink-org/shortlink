# Common default values for dashboard.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

kubernetes-dashboard:
  enabled: true

  fullnameOverride: 'kubernetes-dashboard'

  rbac:
    clusterAdminRole: true

