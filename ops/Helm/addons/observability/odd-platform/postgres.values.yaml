postgresql:
  enabled: true

  global:
    postgresql:
      auth:
        postgresPassword: odd-platform
        username: odd-platform
        password: odd-platform
        database: odd-platform
    loadBalancerSourceRanges: ""
    platformServiceType: ""

  primary:
    persistence:
      enabled: false
