loki:
  enabled: true

  loki:
    deployment:
      isSingleBinary: true

    commonConfig:
      replication_factor: 1

    storage:
      type: filesystem

  write:
    replicas: 0
    persistence:
      storageClass: "-"

  read:
    replicas: 0
    persistence:
      storageClass: "-"

  backend:
    replicas: 0

  tableManager:
    enabled: true

  monitoring:
    selfMonitoring:
      enabled: false

  grafana-agent-operator:
    image:
      tag: v0.30.2


