loki:
  enabled: true

  loki:
    deployment:
      isSingleBinary: true

    commonConfig:
      replication_factor: 1

    storage:
      type: filesystem

  test:
    enabled: false

  write:
    replicas: 1
    persistence:
      storageClass: rook-ceph-block

  read:
    replicas: 1
    persistence:
      storageClass: rook-ceph-block

  singleBinary:
    persistence:
      storageClass: rook-ceph-block

  tableManager:
    enabled: true

  monitoring:
    selfMonitoring:
      enabled: false

  grafana-agent-operator:
    image:
      tag: v0.30.2


