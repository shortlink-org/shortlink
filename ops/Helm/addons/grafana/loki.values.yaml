loki:
  enabled: true

  storage:
    type: filesystem

  write:
    replicas: 0
    persistence:
      storageClass: "-"

  read:
    replicas: 0
    persistence:
      storageClass: "-"

  backend:
    replicas: 1


