apiVersion: v1
kind: ConfigMap
metadata:
  name: redis-dashboard
  labels:
    grafana_dashboard: "true"
  annotations:
    grafana_dashboard_folder: Data Base
data:
  redis-dashboard.json.url: "https://raw.githubusercontent.com/oliver006/redis_exporter/master/contrib/grafana_prometheus_redis_dashboard.json"
