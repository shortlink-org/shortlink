apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: shortlink-
spec:
  entrypoint: whalesay
  templates:
    - name: whalesay
      container:
        image: docker/whalesay
        command: [cowsay]
        args: ["Hello, world!"]
