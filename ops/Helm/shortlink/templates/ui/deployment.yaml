{{- if .Values.ui.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "shortlink.fullname" . }}-ui
  labels:
    {{ include "shortlink.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.ui.deploy.replicaCount }}
  strategy:
    {{- toYaml .Values.ui.deploy.strategy | nindent 4 }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "shortlink.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: ui
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "shortlink.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: ui
    spec:
    {{- with .Values.ui.deploy.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | indent 8 }}
    {{- end }}
      serviceAccountName: {{ template "shortlink.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.ui.deploy.podSecurityContext | indent 8 }}
      containers:
      - name: {{ .Chart.Name }}
        securityContext:
          {{- toYaml .Values.ui.deploy.securityContext | indent 12 }}
        image: "{{ .Values.ui.deploy.image.repository }}:{{ .Values.ui.deploy.image.tag }}"
        imagePullPolicy: {{ .Values.ui.deploy.image.pullPolicy }}
        ports:
        - name: http
          containerPort: 80
          protocol: TCP
        livenessProbe:
          {{- toYaml .Values.ui.deploy.livenessProbe | nindent 12 }}
        readinessProbe:
          {{- toYaml .Values.ui.deploy.readinessProbe | nindent 12 }}
        resources:
          {{- toYaml .Values.ui.deploy.resources | indent 10 }}
      {{- with .Values.ui.deploy.nodeSelector }}
      nodeSelector:
        {{- toYaml . | indent 8 }}
      {{- end }}
    {{- with .Values.ui.deploy.affinity }}
      affinity:
        {{- toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.ui.deploy.tolerations }}
      tolerations:
        {{- toYaml . | indent 8 }}
    {{- end }}
{{- end }}
