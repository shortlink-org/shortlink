{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "shortlink.serviceAccountName" . }}
  labels:
{{ include "shortlink.labels" . | indent 4 }}
{{- end -}}
