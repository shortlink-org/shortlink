name: "apple_goods_policy"
rules:
  - name: "check if goods are from ${brand}"
    condition: "brand == brand"
    message: "Error: Only goods from ${brand} are allowed"
    action: "allow"
params:
  brand: "Apple"
